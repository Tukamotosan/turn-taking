# turn-taking

## Setup dependencies

### Docker

You can build and run docker environment.

```bash
$ ./docker/build.sh
$ ./docker/run.sh
```

This project directory is mounted on `/home/ubuntu/turn-taking`.
